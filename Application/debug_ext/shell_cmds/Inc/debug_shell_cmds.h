#ifndef DEBUG_SHELL_CMDS_H
#define DEBUG_SHELL_CMDS_H

// Standard shell commands (help, ver, sys, log, reset) — optional extension package.
// Register via debug_shell_init() in the core shell module.

#endif
